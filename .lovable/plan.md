In `CandidaturaDocPreview.tsx`, shift the toolbar action buttons ("Pessoas que visualizam", "Imprimir", "Descarregar") from the far right edge toward the left by replacing `justify-between` with `justify-start gap-4` in the toolbar container. This collapses the empty middle space and places the buttons immediately after the left-side ID badge and label.

After the change, take a screenshot of the preview to verify the new spacing looks correct.