// GAP discente profile reuses the institutional Admin profile so any change
// in the admin module reflects here and vice-versa (single source of truth).
export { default } from "@/pages/admin/DiscenteProfile";
